language: ruby

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.1

branches:
  only:
    - master

notifications:
  irc:
    channels:
      - "irc.freenode.net#mashiro-dev" 
    use_notice: true
    skip_join: true

